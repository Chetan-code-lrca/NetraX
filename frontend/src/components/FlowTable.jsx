import StatusBadge from './StatusBadge'
function FlowTable({ alerts, selectedAlertId, onSelectAlert }) { return <section className="section" aria-labelledby="flows-title"><div className="section-heading"><div><h2 id="flows-title">Recent observed flows</h2><p>Select a flow to inspect its evidence and observation limits.</p></div><span className="section-note">Demo data · API-ready</span></div><div className="table-wrap"><table className="flow-table"><thead><tr><th>Timestamp</th><th>Flow ID</th><th>Source</th><th>Destination</th><th>Threat</th><th>Confidence</th><th>Status</th></tr></thead><tbody>{alerts.map((alert) => <tr className={selectedAlertId === alert.flow_id ? 'selected' : ''} key={alert.flow_id}><td>{alert.timestamp}</td><td><button className="flow-button" type="button" onClick={() => onSelectAlert(alert.flow_id)} aria-label={`Inspect ${alert.flow_id}`}>{alert.flow_id}</button></td><td>{alert.source ?? '—'}</td><td>{alert.destination ?? '—'}</td><td className="threat-name">{alert.threat_class}</td><td className="confidence">{alert.confidence}</td><td><StatusBadge status={alert.status} /></td></tr>)}</tbody></table></div></section> }
export default FlowTable
