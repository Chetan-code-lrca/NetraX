const stages = [['01', 'PCAP', 'Passive packet capture'], ['02', 'Flow Extraction', 'One-way flow records'], ['03', 'Feature Extraction', 'Observable traffic features'], ['04', 'Threat Models', 'Detection-specific analysis'], ['05', 'Separability Engine', 'Decision limits from available evidence'], ['06', 'Alert Schema', 'Evidence-aware alert record'], ['07', 'Dashboard', 'Operator review and investigation']]
const models = ['PortScan', 'DDoS', 'C2', 'DNS/DGA', 'DNS Tunnel', 'Encrypted Malware', 'Data Exfiltration']
function DetectionPipeline() { return <section className="section pipeline" aria-labelledby="pipeline-title"><div className="section-heading"><div><h2 id="pipeline-title">NetraX architecture</h2><p>Passive analysis path for captured one-way traffic.</p></div></div><div className="pipeline-track">{stages.map(([number, title, description]) => <div className="pipeline-stage" key={title}><span className="stage-number">{number}</span><h3>{title}</h3><p>{description}</p>{title === 'Threat Models' && <div className="model-list">{models.map((model) => <span key={model}>{model}</span>)}</div>}</div>)}</div></section> }
export default DetectionPipeline
