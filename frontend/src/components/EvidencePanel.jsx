function EvidencePanel({ alert }) { return <section className="evidence-panel" aria-labelledby="evidence-title"><h4 id="evidence-title">Evidence and observability</h4><p className="observability-copy"><strong>{alert.observability_status}:</strong> {alert.observability}</p><div className="evidence-columns"><div className="evidence-group observed"><h5>Observed evidence</h5>{alert.evidence.length ? <ul>{alert.evidence.map((item) => <li key={item}>{item}</li>)}</ul> : <p>Not supplied by analysis service.</p>}</div><div className="evidence-group unavailable"><h5>Unavailable / missing evidence</h5>{alert.missing_evidence.length ? <ul>{alert.missing_evidence.map((item) => <li key={item}>{item}</li>)}</ul> : <p>Not supplied by analysis service.</p>}</div></div></section> }
export default EvidencePanel
